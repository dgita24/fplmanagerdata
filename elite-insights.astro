<template>
  <section>
    <h2>Player Insights</h2>
    <table>
      <thead>
        <tr>
          <th>Position</th>
          <th>Bonus Points</th>
          <th>Other Points</th>
          <th>Hits Cost (Avg per Manager Season)</th>
        </tr>
      </thead>
      <tbody>
        <!-- Removed TOTAL POINTS row -->
        <tr v-for="player in players" :key="player.id">
          <td>{{ player.position }}</td>
          <td>{{ player.bonus_points }}</td>
          <td>{{ player.other_points }}</td>
          <td>{{ (player.avg_hits_count * 4).toFixed(2) }}</td> <!-- Corrected hits cost calculation -->
        </tr>
      </tbody>
    </table>
  </section>
</template>

<script>
export default {
  props: {
    players: Array
  }
};
</script>
